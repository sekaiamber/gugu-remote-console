# Info 模块

## Path

```javascript
const path = `infos/${SCREENID}`;
```

## Example

```shell
features
  $SCREENID
    device: "..."
    location: "..."
    userAgent: "..."
```

## Remark

`features`下的所有属性，均为JSON字符串
